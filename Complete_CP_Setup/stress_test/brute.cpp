// brute.cpp
#include <bits/stdc++.h>
using namespace std;

extern void solve();

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t; cin >> t;
    while (t--) solve();

    return 0;
}
